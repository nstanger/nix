{
    # Enable plugins.
    EnableBundles = 1;
    BundleCompatibilityVersion = 4;
    # Display emails in threaded mode, sorted by date (newest at the top)
    InboxViewerAttributes = {
        DisplayInThreadedMode = "YES";
        SortedDescending = "YES";
        SortOrder = "received-date";
    };
}
